# Polvo environment (copy to .env)
# Upstream Ollama Cloud key — required to reach https://ollama.com/v1
OLLAMA_API_KEY=your_key_here

# Optional overrides
# OLLAMA_BASE_URL=https://ollama.com/v1
# ROUTER_HOST=127.0.0.1
# ROUTER_PORT=9000
# ROUTER_DEFAULT_TIER=air
# ROUTER_MIN_CLASSIFY_LEN=20
# ROUTER_API_KEY=  # optional bearer token clients must send to reach the router
# ROUTER_MODELS_YAML=  # optional path to a custom models YAML (default: ~/.config/polvo/config.yml, then router.models.yaml)

# Multi-provider: if you add a provider to router.models.yaml with a different
# api_key_env, define that key here too. Example (Gemini):
# GEMINI_API_KEY=your_gemini_key_here
